package;


/**
* Time periods for date math
*
*/
enum EDateTime {

    Year(n:Int);
    Month(n:Int);
    Day(n:Int);
    Hour(n:Int);
    Minute(n:Int);
    Second(n:Int);

    Week(n:Int);

}//enum EDateTime